apiVersion: consul.hashicorp.com/v1alpha1
kind: ServiceIntentions
metadata:
  name: dc1-nginx-ssl
spec:
  destination:
    name: dc1-nginx-ssl
  sources:
    - name: static-client
      action: allow

